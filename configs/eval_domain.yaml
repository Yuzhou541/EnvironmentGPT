seed: 42

model:
  name_or_path: "REPLACE_ME"
  load_in_4bit: false
  torch_dtype: "float16"
  attn_implementation: "sdpa"

eval:
  domain_path: "data/processed/envqa_test.jsonl"
  general_path: "data/processed/general_anchors.jsonl"
  checkpoint_path: "REPLACE_ME"
  max_new_tokens: 256
  temperature: 0.0
  top_p: 1.0
  batch_size: 1

metrics: ["exact_match","token_f1","numeric"]
